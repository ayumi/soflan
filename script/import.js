// Imports a directory of simfiles, converting them, upserting into the DB, and updating the song list.
// Usage: import.js [Directory containing simfiles]

import _ from "lodash";

console.log("goodbye world");
